export namespace SharefileNodeAPI {
    export interface Auth {
        subdomain   : string;
        username    : string;
        password    : string;
        clientId    : string;
        clientSecret: string;
    }
}